<%= render partial: 'shared/leaves_header', locals: { heading: t('user_leave.headings.user_leaves') } %>
<div class="container">
  <h1><%= t('user_leave.headings.user_leaves') %></h1>
  <div id="user_leaves_table">
    <%= render partial: 'user_leaves_list', locals: { user_leaves: @user_leaves } %>
  </div>
</div>