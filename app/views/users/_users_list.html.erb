<table class='table table-striped table-hover'>
  <thead class='table-dark'>
    <th><%= t('forms.labels.full_name') %></th>
    <th colspan='2'><%= t('forms.labels.email') %></th>
  </thead>
  <tbody>
    <% users.each do |user| %>
      <tr>
        <td><%= user.full_name %></td>
        <td><%= user.email %></td>
        <td>
          <%= link_to t('forms.buttons.details'), member_path(user), class:'btn btn-outline-secondary' if can?(:read, user) %>
          <%= link_to t('forms.buttons.edit'), edit_member_path(user), class:'btn btn-outline-secondary' if can?(:update, user) %>
          <%= link_to t('forms.buttons.delete'), member_path(user), method: :delete,
                                                data: { confirm: t('messages.confirmation') }, 
                                                class:'btn btn-danger' if can?(:destroy, user) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<<<<<<< HEAD
<div class='js-pagination-wrapper'>
=======
<div class='js-pagination-wrapper pagination-wrapper'>
>>>>>>> 94ea65e5afbfd092dcb563aa29cfd416c80a17ca
  <%= will_paginate users, container: false %>
</div>
