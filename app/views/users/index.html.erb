<h1>All Employees</h1>
<% @users.each do |user| %>
  <h1><%= user.first_name %> <%= link_to 'Edit', edit_member_path(user) %> <%= link_to 'Destroy', member_path(user) , method: :delete, data: {confirm: 'Are you sure?'} %> <%= link_to 'Details', member_path(user) %> </h1>
<% end %>

<%= link_to 'New Employee', new_member_path %>