<div class="row g-3 mt-1">
  <div class="col-xl-6">
    <%= render(CardComponent.new) do %>
      <%= column_chart employees_per_department_dashboard_index_path,
                       title: t('chartkick.employee_per_dept'), loading: t('chartkick.loading') %>
    <% end %>
  </div>
  <div class="col-xl-6">
    <%= render(CardComponent.new) do %>
      <%= bar_chart employees_per_city_dashboard_index_path,
                    title: t('chartkick.employee_per_city'), loading: t('chartkick.loading') %>
    <% end %>
  </div>
</div>
