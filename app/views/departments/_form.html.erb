<div class="container">
  <div class="row mt-4">
    <div class="col-4">
      <h1><%= local_assigns&.dig(:heading) %></h1>
      <%= form_for @department do |f| %>
        <div class="form-group mb-3">
          <%= f.label t 'department.name_lable' %>
          <%= f.text_field :name, required: true, class: 'form-control' %>
        </div>
        <div class="input-group mb-3 row">
          <div class="col-md-6">
            <%= f.label t 'department.image_lable' %></br>
            <%= f.file_field :image_url %>
          </div>
        </div>
        <div>
          <%= f.submit class: 'card-link btn btn-dark' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
