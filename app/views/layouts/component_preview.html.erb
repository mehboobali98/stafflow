<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <%= render "layouts/head" %>
  <body>
    <%# Deliberately none of the app chrome. The default layout reaches for
        current_user, which a preview has no request to supply. %>
    <div class="p-4"><%= yield %></div>
  </body>
</html>
