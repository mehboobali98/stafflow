<% if notifications.nil? || notifications.empty? %>
  <h5 class='p-3'>No New Notifications</h5>
<% else %>
  <% notifications.each do |notification| %>
    <hr>
    <a class="p-3" href="#"> 
      <div> 
        <h5><%= notification.body %></h5>  
      </div> 
    </a> 
  <% end %>
<% end %>