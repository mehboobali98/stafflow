<div class="container">
  <div class="row justify-content-center">
    <div class="col-4">
      <h1> <%= heading%></h1>
      <% if leave_type.errors.any? %>
        <div>
          <ul>
            <% leave_type.errors.full_messages.each do |message| %>
              <li class='text-danger'><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <%= form_with model: leave_type, local:true, :html => {'class' => 'card p-3 bg-light'} do |form| %>
        <div class="form-group mb-3">
          <%= form.label :name, 'Leave type name ' %>
          <%= form.text_field :name, required: true, class: 'form-control', placeholder: "Enter leave type name"%>
        </div>
        <div class="form-group mb-3">
          <%= form.label :count, 'Leave type count' %>
          <%= form.number_field :count, in: 1..20, required: true, class: 'form-control', placeholder: "Enter leave type count"%>
        </div>
        <div>
          <%= form.submit class: 'card-link btn btn-primary'%>
        </div>
      <% end %>
    </div>
  </div>
</div>