<div class="contrainer">
  <div class="row justify-content-center">
    <div class="col-md-4">
      <div class="card text-center">
        <div class="card-header">
          Featured Leave Type
        </div>
        <div class="card-body">
          <h1 class="card-title"><%= @leave_type.name %></h1>
          <p class="card-text"><%= @leave_type.count %></p>
        </div>
        <div class="card-footer">
          <%= link_to "Edit", edit_leave_type_path(@leave_type), class: 'btn btn-secondary'%>
          <%= link_to "Destroy", leave_type_path(@leave_type), class: 'btn btn-danger',
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
          <%= link_to "View all leave types", leave_types_path, class: 'btn btn-outline-primary' %>
        </div>
      </div>
    </div>
  </div>
</div>